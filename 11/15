/* №15. Реализовать предикат с помощью рекурсии вверх.
Найти произведение цифр числа. */

mult(X,X):- X<10,!.
mult(X,Y):-
           X1 is X div 10,
           mult(X1,Y1),
           Y is Y1*(X mod 10).
