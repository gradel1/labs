/* №11. Построить предикат father(X, Y), который проверяет,
является ли X отцом Y. Построить предикат, father(X), который выводит
отца X. */

father(X,Y):- parent(X,Y),man(X).
father(Y):- parent(X,Y),man(X),write(X).
